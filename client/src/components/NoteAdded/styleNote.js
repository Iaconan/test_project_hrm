import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  
  card: {
    display: 'flex',
    justifyContent: 'center',
    // borderRadius: '15px',
    height: '160px',
    position: 'relative',
  },
  title: {
    display: 'flex',
    justifyContent: 'center',
    // borderRadius: '15px',
    height: '1px',
    // position: 'relative',
  }
});
